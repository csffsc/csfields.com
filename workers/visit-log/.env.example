# Copy to .env.local (gitignored) for Cursor agent and other non-interactive shells.
#
# Infisical → project csfields → Service Tokens → Create
#   environment: prod
#   path: /visit-log
#   permission: read
INFISICAL_TOKEN=
